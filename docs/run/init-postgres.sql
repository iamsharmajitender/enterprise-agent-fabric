-- Extra databases on the single Postgres 16 server.
-- POSTGRES_DB already created `afd`. Do not create `acp` (Control Plane has no database).

CREATE DATABASE adp;
CREATE DATABASE ar;
CREATE DATABASE acr;
