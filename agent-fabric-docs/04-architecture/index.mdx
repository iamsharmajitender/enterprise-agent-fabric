---
title: "Enterprise Agent Fabric — architecture"
sidebar_label: Architecture index
sidebar_position: 0
description: "Index for Fabric box architecture MDX. Diagrams live in diagrams/."
---

# Enterprise Agent Fabric — solution architecture

Box design docs for the Fabric. Each file is MDX with frontmatter, `gain-diagram-wrap` figures, and the full solution pack (deployment, APIs, data, sequences, failure).

These packs may be ahead of code. Behaviour of the running binary is [02-understand/status.md](../02-understand/status.md).

**Status:** Draft, aligned to the pin-split proposal (AFD pins and starts; Agent Plane classifies; AR runs).

## Documents

| Pack | Box | Job in one line |
| --- | --- | --- |
| [Enterprise Agent Fabric](./enterprise-agent-fabric-architecture.mdx) | Whole fabric | One decide contract, two AFD fleets, three principals |
| [Enterprise Agent Fabric V1](./enterprise-agent-fabric-pitch.mdx) | Executive brief | One front door, many governed capabilities |
| [Agent Front Door](./agent-front-door.mdx) | Chat AFD + API AFD | Only way in. Entitle, freeze, start. Two fleets, one contract. |
| [Agent Plane](./agent-plane.mdx) | ACP + Agent Data Plane | Catalogue and classify. Does not pin or start AR. |
| [Agent Runtime](./agent-runtime.mdx) | AR + Shared calls | Pin, hydrate, LangGraph loop, Patterns 0–3. |
| [Agent Capability Registry](./agent-capability-registry.mdx) | Registry (with Agent Plane) | Publishers append `id@version`. AR hydrates at pin. |

Read order matches a request: **Fabric → AFD → Plane → Runtime → Registry**.

## Diagrams

All figures live in [`diagrams/`](./diagrams/) (SVG for MDX, HTML as editorial source). Nothing except `.mdx` files sits in this folder root.

## Locked fabric rules

1. **Four jobs.** AFD entitles, pins, starts. Data Plane classifies. ACP is control + audit. AR runs.
2. **Two AFD fleets, one contract.** Separate process (FR-9). `{runs_topic}` is AR return.
3. **Jobs name `route_id`.** Skip classifier and clarify. Do not skip entitle + record.
4. **Generic chat still classifies.** Hints are eligible + chat-visible.
5. **Only `route` starts AR.** Clarify / abstain never pin. Default start is async `202`.
6. **One Agent Plane per trust domain.** Not one mega-agent.
7. **Three principals.** User ≠ agent ≠ AR workload.
8. **Capabilities are references.** AR hydrates the whole pinned manifest before the LLM.
